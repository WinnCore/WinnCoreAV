[package]
name = "av-ebpf-common"
version = "0.1.0"
edition = "2021"
description = "Shared types for WinnCore eBPF probes and userspace"
publish = false

[features]
default = []
user = ["serde"]
bpf = []

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }
aya = { version = "0.12", optional = true }

[lib]
path = "src/lib.rs"
