//! WinnCoreAV desktop interface crate.
//!
//! The Tauri-based `av-gui` binary is gated behind the `gui` feature so the
//! full workspace can build in headless environments without GTK/WebKit
//! development libraries installed.
