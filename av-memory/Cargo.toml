[package]
name = "av-memory"
version = "0.1.0"
edition = "2021"
description = "Fileless malware and memory scanning for WinnCore"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"
thiserror = "1.0"
procfs = "0.16"
memmap2 = "0.9"
regex = "1"
