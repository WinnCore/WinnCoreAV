# Detection Baselines

This directory stores detection baselines used for Negative Flip Rate (NFR) checks.

Generate/update with:

```bash
./scripts/update_detection_baseline.sh
```

Until a baseline is generated, regression checks will warn and may be skipped depending on flags.
